{
  "name": "pokemon-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build && cd .. && mkdir -p public && cp -r client/build/* public/"
  },
  "dependencies": {
    "express": "^4.17.1",
    "pg": "^8.5.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.3.5",
    "dotenv": "^8.2.0",
    "cors": "^2.8.5"
  }
}
